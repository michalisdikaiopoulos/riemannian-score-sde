enabled: false

# Unsafe region definition. Two mutually exclusive shapes, picked via `region`:
#   "wedge"     (default) - azimuthal wedge on S2, mirrored at +pi to give two
#                opposite wedges on the ring. Uses phi_min/phi_max.
#   "component" - geodesic cap of radius `cap_radius` around the mean direction
#                of Kent mixture component `unsafe_component` (requires a
#                dataset exposing `.means`, e.g. KentSynthetic).
region: wedge
phi_min: -0.3
phi_max: 0.3
unsafe_component: 0  # index into dataset.means (region: component)
cap_radius: 0.5       # geodesic radius in radians (region: component)
target_n: 30  # total reference points collected across the unsafe region

method: early_window
rejection_radius: 0.5  # geodesic radius (radians) for noise rejection

# Score correction
eta: 5.0      # overall strength of the safety correction
beta_max: 10.0 # hard cap on beta to prevent blow-up
t_min: 0.0    # early_window: disable guardrail below this t; late_window_scaled: lower bound of active window
t_max: 0.2     # late_window_scaled: upper bound of active window

# Heat kernel approximation
n_max: 5       # number of terms in the series expansion
